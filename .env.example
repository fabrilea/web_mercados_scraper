# Copiar a .env para correr localmente. En GitHub Actions esto se carga desde
# Settings > Secrets and variables > Actions > DATABASE_URL (ver README.md).
DATABASE_URL="postgresql://usuario:password@host:5432/nombre_db?sslmode=require"
